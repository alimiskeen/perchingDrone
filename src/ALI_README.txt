

The ard_ser_relay node needs pyserial installed, use the command
  pip3 install pyserial

The testmessagesender node is just to test the ard_ser_relay node. It can be completely ignored if you want it to. 
